
import * as React from 'react';
import { ScrollView, Text } from "react-native"



export const Settings = () => {
    return (
        <ScrollView>
            <Text>Settings</Text>
        </ScrollView>

    )
}